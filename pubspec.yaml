name: bsm_servicos_app

description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+2

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.1
  shared_preferences: ^2.2.3
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1
  url_launcher: ^6.3.0
  share_plus: ^10.0.2

  # ⭐ NOVO
  smooth_page_indicator: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true